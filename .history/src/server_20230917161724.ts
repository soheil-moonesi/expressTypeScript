import express, { Request, Response, NextFunction } from "express";
let app :express.Application
function initServer() {
    const app = express();
}
function connectDb() {}
function initRoute() {}
